//


module.exports = function(arrayOfObjs = []) {
  let returnObj
  for (let i = 0; i < arrayOfObjs.length; i++) {
    //loop on each property, check if it is already there, and handle it appropriately
  }
  return returnObj
}
